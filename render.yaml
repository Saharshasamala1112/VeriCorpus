services:
  - type: web
    name: vericorpus-api
    runtime: python
    rootDir: client_app
    buildCommand: pip install -r backend/requirements.txt
    startCommand: alembic upgrade head && uvicorn app.main:app --app-dir backend --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: DEBUG
        value: "false"
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        sync: false
      - key: CORPUS_DB_URL
        sync: false
      - key: CORS_ORIGINS
        value: "http://localhost:5173,https://localhost:5173,https://veri-corpus-12cj08ydc-saharshasamala1112s-projects.vercel.app"
      - key: GEMINI_API_KEY
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: FORENSIC_ENABLED
        value: "false"
      - key: CORPUS_BASE_URL
        value: "https://api.corpus.swecha.org"
      - key: CORPUS_PHONE
        sync: false
      - key: CORPUS_PASSWORD
        sync: false

  - type: web
    name: vericorpus-frontend
    runtime: static
    rootDir: client_app/frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_BASE_URL
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html